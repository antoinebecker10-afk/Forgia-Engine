# forgia-pp-rain

> Screen rain droplets

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_rain::ForgiaPpRainPlugin;
app.add_plugins(ForgiaPpRainPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
