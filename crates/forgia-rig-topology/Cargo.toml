[package]
name = "forgia-rig-topology"
version = "0.1.0"
edition = "2021"
description = "Procedural rig classification — bones tagged leg/arm/spine/tail by 3D heuristics, rig-agnostic (Meshy/AccuRig/Mixamo/Blender)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
