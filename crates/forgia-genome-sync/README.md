# forgia-genome-sync

> Genome -> FpsTuning sync macro

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_sync::ForgiaGenomeSyncPlugin;
app.add_plugins(ForgiaGenomeSyncPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
