# forgia-qa-invariants

> Runtime invariant checks (CHECK 1-95)

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_invariants::ForgiaQaInvariantsPlugin;
app.add_plugins(ForgiaQaInvariantsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
