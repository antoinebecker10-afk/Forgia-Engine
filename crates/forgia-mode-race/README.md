# forgia-mode-race

> Racing mode plugin

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_race::ForgiaModeRacePlugin;
app.add_plugins(ForgiaModeRacePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
