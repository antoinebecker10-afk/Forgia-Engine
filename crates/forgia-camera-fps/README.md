# forgia-camera-fps

> FPS camera (1P, head-bob)

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_camera_fps::ForgiaCameraFpsPlugin;
app.add_plugins(ForgiaCameraFpsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
