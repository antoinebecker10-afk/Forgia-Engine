# forgia-pp-night-vision

> Night vision green tint + noise

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_night_vision::ForgiaPpNightVisionPlugin;
app.add_plugins(ForgiaPpNightVisionPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
