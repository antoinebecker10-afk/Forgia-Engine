# forgia-status-effects

> Burn/slow/stun/poison status with stacking

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_status_effects::ForgiaStatusEffectsPlugin;
app.add_plugins(ForgiaStatusEffectsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
