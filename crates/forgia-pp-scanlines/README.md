# forgia-pp-scanlines

> Horizontal scanlines

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_scanlines::ForgiaPpScanlinesPlugin;
app.add_plugins(ForgiaPpScanlinesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
