# forgia-editor-kit-macros

> Inspectable derive + registry macros

**Category** : `macros` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_kit_macros::ForgiaEditorKitMacrosPlugin;
app.add_plugins(ForgiaEditorKitMacrosPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
