//! forgia-audio-occlusion
//!
//! 3D audio occlusion (walls muffle)
//!
//! Category : audio

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaAudioOcclusionPlugin).
pub struct ForgiaAudioOcclusionPlugin;

impl Plugin for ForgiaAudioOcclusionPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
