# forgia-audio-occlusion

> 3D audio occlusion (walls muffle)

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_occlusion::ForgiaAudioOcclusionPlugin;
app.add_plugins(ForgiaAudioOcclusionPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
