# forgia-pp-halftone

> Halftone dot pattern

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_halftone::ForgiaPpHalftonePlugin;
app.add_plugins(ForgiaPpHalftonePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
