# forgia-pp-crt

> CRT TV scanline + curvature

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_crt::ForgiaPpCrtPlugin;
app.add_plugins(ForgiaPpCrtPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
