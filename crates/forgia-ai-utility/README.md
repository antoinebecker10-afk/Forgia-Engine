# forgia-ai-utility

> Utility AI (curves + scorers)

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_utility::ForgiaAiUtilityPlugin;
app.add_plugins(ForgiaAiUtilityPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
