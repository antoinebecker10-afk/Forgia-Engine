# forgia-player-tp

> 3P RPG controller (orbit camera)

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_player_tp::ForgiaPlayerTpPlugin;
app.add_plugins(ForgiaPlayerTpPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
