# forgia-marketplace-client

> Marketplace client (V2 igc-server)

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_marketplace_client::ForgiaMarketplaceClientPlugin;
app.add_plugins(ForgiaMarketplaceClientPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
