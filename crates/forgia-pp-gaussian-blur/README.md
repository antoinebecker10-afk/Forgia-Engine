# forgia-pp-gaussian-blur

> Gaussian blur

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_gaussian_blur::ForgiaPpGaussianBlurPlugin;
app.add_plugins(ForgiaPpGaussianBlurPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
