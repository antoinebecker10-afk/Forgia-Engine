# forgia-net-replication-genome

> Genome-aware replication policies

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_replication_genome::ForgiaNetReplicationGenomePlugin;
app.add_plugins(ForgiaNetReplicationGenomePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
