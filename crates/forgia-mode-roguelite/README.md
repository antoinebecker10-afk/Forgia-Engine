# forgia-mode-roguelite

> Roguelite mode plugin (procedural)

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_roguelite::ForgiaModeRoguelitePlugin;
app.add_plugins(ForgiaModeRoguelitePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
