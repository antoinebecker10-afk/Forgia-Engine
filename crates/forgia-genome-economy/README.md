# forgia-genome-economy

> XP/loot/price curves genome (economy-designer)

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_economy::ForgiaGenomeEconomyPlugin;
app.add_plugins(ForgiaGenomeEconomyPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
