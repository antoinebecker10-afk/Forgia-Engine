# forgia-input-keybind

> Keybind registry + AZERTY default

**Category** : `input` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_input_keybind::ForgiaInputKeybindPlugin;
app.add_plugins(ForgiaInputKeybindPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
