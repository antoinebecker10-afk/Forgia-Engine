# forgia-ik

> IK (foot placement, look-at)

**Category** : `animation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ik::ForgiaIkPlugin;
app.add_plugins(ForgiaIkPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
