//! forgia-ik
//!
//! IK (foot placement, look-at)
//!
//! Category : animation

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaIkPlugin).
pub struct ForgiaIkPlugin;

impl Plugin for ForgiaIkPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
