# forgia-loot-tables

> Drop tables genome-driven (rarity)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_loot_tables::ForgiaLootTablesPlugin;
app.add_plugins(ForgiaLootTablesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
