# forgia-pp-fog

> Screen-space fog overlay

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_fog::ForgiaPpFogPlugin;
app.add_plugins(ForgiaPpFogPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
