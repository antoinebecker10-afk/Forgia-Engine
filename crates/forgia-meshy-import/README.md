# forgia-meshy-import

> Meshy AI GLB importer

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_meshy_import::ForgiaMeshyImportPlugin;
app.add_plugins(ForgiaMeshyImportPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
