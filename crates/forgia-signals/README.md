# forgia-signals

> Signal contracts registry inter-crates

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_signals::ForgiaSignalsPlugin;
app.add_plugins(ForgiaSignalsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
