# forgia-trails

> Motion trails (sword, projectile)

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_trails::ForgiaTrailsPlugin;
app.add_plugins(ForgiaTrailsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
