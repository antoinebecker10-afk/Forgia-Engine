# forgia-pp-heat-haze

> Heat haze distortion

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_heat_haze::ForgiaPpHeatHazePlugin;
app.add_plugins(ForgiaPpHeatHazePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
