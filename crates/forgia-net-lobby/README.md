# forgia-net-lobby

> Lobby system (create/join/list)

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_lobby::ForgiaNetLobbyPlugin;
app.add_plugins(ForgiaNetLobbyPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
