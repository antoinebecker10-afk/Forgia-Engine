//! forgia-net-lobby
//!
//! Lobby system (create/join/list)
//!
//! Category : network

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaNetLobbyPlugin).
pub struct ForgiaNetLobbyPlugin;

impl Plugin for ForgiaNetLobbyPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
