# forgia-import-usd

> USD import (V2)

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_import_usd::ForgiaImportUsdPlugin;
app.add_plugins(ForgiaImportUsdPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
