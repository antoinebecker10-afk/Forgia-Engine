# forgia-genome-ai-personality

> AI personality traits (aggression, fear)

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_ai_personality::ForgiaGenomeAiPersonalityPlugin;
app.add_plugins(ForgiaGenomeAiPersonalityPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
