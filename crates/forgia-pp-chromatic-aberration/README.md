# forgia-pp-chromatic-aberration

> Chromatic aberration RGB shift

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_chromatic_aberration::ForgiaPpChromaticAberrationPlugin;
app.add_plugins(ForgiaPpChromaticAberrationPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
