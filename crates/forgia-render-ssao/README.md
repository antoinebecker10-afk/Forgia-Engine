# forgia-render-ssao

> Screen-space ambient occlusion

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_ssao::ForgiaRenderSsaoPlugin;
app.add_plugins(ForgiaRenderSsaoPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
