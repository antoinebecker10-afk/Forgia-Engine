# forgia-genome-core

> TOML loader + hot-reload core (Forgia unique)

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_core::ForgiaGenomeCorePlugin;
app.add_plugins(ForgiaGenomeCorePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
