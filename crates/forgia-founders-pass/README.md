# forgia-founders-pass

> Founders pass entitlement check

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_founders_pass::ForgiaFoundersPassPlugin;
app.add_plugins(ForgiaFoundersPassPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
