# forgia-ui-dialogue

> Dialogue UI (choices, portrait)

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_dialogue::ForgiaUiDialoguePlugin;
app.add_plugins(ForgiaUiDialoguePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
