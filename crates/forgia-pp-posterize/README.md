# forgia-pp-posterize

> Posterize (quantize levels)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_posterize::ForgiaPpPosterizePlugin;
app.add_plugins(ForgiaPpPosterizePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
