# forgia-animation-blend

> Animation blend state machine

**Category** : `animation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_animation_blend::ForgiaAnimationBlendPlugin;
app.add_plugins(ForgiaAnimationBlendPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
