# forgia-ui-inventory

> Inventory panel UI (80 slots pagination)

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_inventory::ForgiaUiInventoryPlugin;
app.add_plugins(ForgiaUiInventoryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
