[package]
name = "forgia-system-sets"
version = "0.1.0"
edition = "2021"
description = "GameSet enum + ordering chain (Input->Physics->...)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
