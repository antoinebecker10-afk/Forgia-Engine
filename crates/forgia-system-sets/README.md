# forgia-system-sets

> GameSet enum + ordering chain (Input->Physics->...)

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_system_sets::ForgiaSystemSetsPlugin;
app.add_plugins(ForgiaSystemSetsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
