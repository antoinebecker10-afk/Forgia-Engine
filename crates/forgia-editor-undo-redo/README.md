# forgia-editor-undo-redo

> Undo/redo command pattern

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_undo_redo::ForgiaEditorUndoRedoPlugin;
app.add_plugins(ForgiaEditorUndoRedoPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
