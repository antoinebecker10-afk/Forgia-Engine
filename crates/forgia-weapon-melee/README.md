# forgia-weapon-melee

> Melee weapon swing arc + capsule cast

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_weapon_melee::ForgiaWeaponMeleePlugin;
app.add_plugins(ForgiaWeaponMeleePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
