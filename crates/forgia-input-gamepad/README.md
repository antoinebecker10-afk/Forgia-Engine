# forgia-input-gamepad

> Gamepad mapping + rumble

**Category** : `input` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_input_gamepad::ForgiaInputGamepadPlugin;
app.add_plugins(ForgiaInputGamepadPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
