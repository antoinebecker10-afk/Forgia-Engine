# forgia-pp-thermal

> Thermal vision palette

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_thermal::ForgiaPpThermalPlugin;
app.add_plugins(ForgiaPpThermalPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
