[package]
name = "forgia-damage"
version = "0.1.0"
edition = "2021"
description = "Health component + DamageEvent system + HitZone types (story-457)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
forgia-genome-core = { workspace = true }
serde = { workspace = true }
