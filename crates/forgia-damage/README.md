# forgia-damage

> Health component + DamageEvent system

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_damage::ForgiaDamagePlugin;
app.add_plugins(ForgiaDamagePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
