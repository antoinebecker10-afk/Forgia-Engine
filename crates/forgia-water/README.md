# forgia-water

> bevy_water wrapper + WaterTiles management

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_water::ForgiaWaterPlugin;
app.add_plugins(ForgiaWaterPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
