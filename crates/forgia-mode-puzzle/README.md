# forgia-mode-puzzle

> Puzzle mode plugin

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_puzzle::ForgiaModePuzzlePlugin;
app.add_plugins(ForgiaModePuzzlePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
