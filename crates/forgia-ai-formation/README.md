# forgia-ai-formation

> Squad formation movement

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_formation::ForgiaAiFormationPlugin;
app.add_plugins(ForgiaAiFormationPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
