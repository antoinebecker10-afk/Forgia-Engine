# forgia-time

> Time<Real>/Time<Virtual> helpers anti-trap UI

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_time::ForgiaTimePlugin;
app.add_plugins(ForgiaTimePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
