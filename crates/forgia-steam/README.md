# forgia-steam

> bevy-steamworks wrapper (story-424)

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_steam::ForgiaSteamPlugin;
app.add_plugins(ForgiaSteamPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
