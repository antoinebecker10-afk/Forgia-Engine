# forgia-net-matchmaking

> Matchmaking queue

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_matchmaking::ForgiaNetMatchmakingPlugin;
app.add_plugins(ForgiaNetMatchmakingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
