# forgia-pp-motion-blur

> Camera motion blur

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_motion_blur::ForgiaPpMotionBlurPlugin;
app.add_plugins(ForgiaPpMotionBlurPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
