# forgia-qa-vlm

> VLM (LLaVA) screenshot oracle

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_vlm::ForgiaQaVlmPlugin;
app.add_plugins(ForgiaQaVlmPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
