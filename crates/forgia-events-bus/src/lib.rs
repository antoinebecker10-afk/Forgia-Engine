//! forgia-events-bus
//!
//! Typed event channels inter-crates
//!
//! Category : foundation

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaEventsBusPlugin).
pub struct ForgiaEventsBusPlugin;

impl Plugin for ForgiaEventsBusPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
