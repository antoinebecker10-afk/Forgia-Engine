# forgia-events-bus

> Typed event channels inter-crates

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_events_bus::ForgiaEventsBusPlugin;
app.add_plugins(ForgiaEventsBusPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
