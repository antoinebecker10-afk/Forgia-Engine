[package]
name = "forgia-ui-menu"
version = "0.1.0"
edition = "2021"
description = "Start menu + pause + settings"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
bevy_egui = { workspace = true }
