# forgia-ui-menu

> Start menu + pause + settings

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_menu::ForgiaUiMenuPlugin;
app.add_plugins(ForgiaUiMenuPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
