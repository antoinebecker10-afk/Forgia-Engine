# forgia-script-variables

> Script-mutable variables registry

**Category** : `scripting` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_script_variables::ForgiaScriptVariablesPlugin;
app.add_plugins(ForgiaScriptVariablesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
