# forgia-pp-cross-hatch

> Cross-hatch shading

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_cross_hatch::ForgiaPpCrossHatchPlugin;
app.add_plugins(ForgiaPpCrossHatchPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
