# forgia-ai-flocking

> Boids flocking behavior

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_flocking::ForgiaAiFlockingPlugin;
app.add_plugins(ForgiaAiFlockingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
