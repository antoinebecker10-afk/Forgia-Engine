# forgia-manifest

> Capability discovery for AI agents (parse manifest.toml)

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_manifest::ForgiaManifestPlugin;
app.add_plugins(ForgiaManifestPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
