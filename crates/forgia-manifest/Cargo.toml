[package]
name = "forgia-manifest"
version = "0.1.0"
edition = "2021"
description = "Capability discovery — parse manifest.toml of all crates for AI assembly"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
