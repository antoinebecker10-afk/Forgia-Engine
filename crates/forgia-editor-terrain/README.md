# forgia-editor-terrain

> Terrain brush sculpt + paint

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_terrain::ForgiaEditorTerrainPlugin;
app.add_plugins(ForgiaEditorTerrainPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
