# forgia-ui-gauges

> Reusable gauges/bars widgets

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_gauges::ForgiaUiGaugesPlugin;
app.add_plugins(ForgiaUiGaugesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
