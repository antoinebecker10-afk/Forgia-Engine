# forgia-ai-blackboard

> Shared blackboard for AI knowledge

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_blackboard::ForgiaAiBlackboardPlugin;
app.add_plugins(ForgiaAiBlackboardPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
