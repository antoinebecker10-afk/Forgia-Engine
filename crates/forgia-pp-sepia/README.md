# forgia-pp-sepia

> Sepia tone

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_sepia::ForgiaPpSepiaPlugin;
app.add_plugins(ForgiaPpSepiaPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
