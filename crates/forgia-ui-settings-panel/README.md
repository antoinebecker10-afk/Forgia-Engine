# forgia-ui-settings-panel

> Settings panel (graphics, audio, controls)

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_settings_panel::ForgiaUiSettingsPanelPlugin;
app.add_plugins(ForgiaUiSettingsPanelPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
