[package]
name = "forgia-ui-settings-panel"
version = "0.1.0"
edition = "2021"
description = "Settings panel (graphics, audio, controls)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
bevy_egui = { workspace = true }
