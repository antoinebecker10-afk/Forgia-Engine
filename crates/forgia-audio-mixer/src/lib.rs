//! forgia-audio-mixer
//!
//! Channel mixer + per-channel volume
//!
//! Category : audio

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaAudioMixerPlugin).
pub struct ForgiaAudioMixerPlugin;

impl Plugin for ForgiaAudioMixerPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
