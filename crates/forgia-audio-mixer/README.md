# forgia-audio-mixer

> Channel mixer + per-channel volume

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_mixer::ForgiaAudioMixerPlugin;
app.add_plugins(ForgiaAudioMixerPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
