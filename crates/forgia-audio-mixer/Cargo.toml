[package]
name = "forgia-audio-mixer"
version = "0.1.0"
edition = "2021"
description = "Channel mixer + per-channel volume"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
bevy_kira_audio = { workspace = true }
