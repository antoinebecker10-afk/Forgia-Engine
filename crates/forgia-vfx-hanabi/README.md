# forgia-vfx-hanabi

> Hanabi VFX wrapper + pre-spawn dummy anti-freeze

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_vfx_hanabi::ForgiaVfxHanabiPlugin;
app.add_plugins(ForgiaVfxHanabiPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
