# forgia-equipment

> Equip slots (weapon, armor, accessory)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_equipment::ForgiaEquipmentPlugin;
app.add_plugins(ForgiaEquipmentPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
