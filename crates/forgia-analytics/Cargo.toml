[package]
name = "forgia-analytics"
version = "0.1.0"
edition = "2021"
description = "Analytics events (funnel)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
