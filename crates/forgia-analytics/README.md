# forgia-analytics

> Analytics events (funnel)

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_analytics::ForgiaAnalyticsPlugin;
app.add_plugins(ForgiaAnalyticsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
