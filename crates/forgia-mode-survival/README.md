# forgia-mode-survival

> Survival mode plugin

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_survival::ForgiaModeSurvivalPlugin;
app.add_plugins(ForgiaModeSurvivalPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
