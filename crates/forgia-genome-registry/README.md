# forgia-genome-registry

> GenomeRegistry trait + catalogue accessors

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_registry::ForgiaGenomeRegistryPlugin;
app.add_plugins(ForgiaGenomeRegistryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
