[package]
name = "forgia-genome-registry"
version = "0.1.0"
edition = "2021"
description = "GenomeRegistry trait + catalogue accessors"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
