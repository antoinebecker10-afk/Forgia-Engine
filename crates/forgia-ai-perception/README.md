# forgia-ai-perception

> Vision/hearing cones + memory

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_perception::ForgiaAiPerceptionPlugin;
app.add_plugins(ForgiaAiPerceptionPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
