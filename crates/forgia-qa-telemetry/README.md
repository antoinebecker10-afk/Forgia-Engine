# forgia-qa-telemetry

> Telemetry collection (24 sensors JSON)

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_telemetry::ForgiaQaTelemetryPlugin;
app.add_plugins(ForgiaQaTelemetryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
