//! forgia-observability
//!
//! Health monitor + lag detector + watchdog
//!
//! Category : qa

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaObservabilityPlugin).
pub struct ForgiaObservabilityPlugin;

impl Plugin for ForgiaObservabilityPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
