# forgia-observability

> Health monitor + lag detector + watchdog

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_observability::ForgiaObservabilityPlugin;
app.add_plugins(ForgiaObservabilityPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
