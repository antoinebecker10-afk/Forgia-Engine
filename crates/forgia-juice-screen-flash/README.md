# forgia-juice-screen-flash

> Screen flash on dmg/heal

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_juice_screen_flash::ForgiaJuiceScreenFlashPlugin;
app.add_plugins(ForgiaJuiceScreenFlashPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
