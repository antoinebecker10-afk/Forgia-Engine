[package]
name = "forgia-mesh-fader"
version = "0.1.0"
edition = "2021"
description = "Mesh material runtime alpha fading (Forgia generic utility — scope lens ADS, windshields, visors, glass panes, despawn fade-out)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
