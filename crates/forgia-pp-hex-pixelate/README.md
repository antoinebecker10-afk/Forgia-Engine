# forgia-pp-hex-pixelate

> Hex pixelation

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_hex_pixelate::ForgiaPpHexPixelatePlugin;
app.add_plugins(ForgiaPpHexPixelatePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
