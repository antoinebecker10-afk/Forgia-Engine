# forgia-player-platformer

> Platformer controller (coyote, jump buffer)

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_player_platformer::ForgiaPlayerPlatformerPlugin;
app.add_plugins(ForgiaPlayerPlatformerPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
