# forgia-anticheat

> Anti-cheat hooks (V2)

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_anticheat::ForgiaAnticheatPlugin;
app.add_plugins(ForgiaAnticheatPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
