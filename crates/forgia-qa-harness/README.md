# forgia-qa-harness

> Headless test harness

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_harness::ForgiaQaHarnessPlugin;
app.add_plugins(ForgiaQaHarnessPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
