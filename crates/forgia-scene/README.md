# forgia-scene

> Scene loader + map_switch + DespawnOnExit

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_scene::ForgiaScenePlugin;
app.add_plugins(ForgiaScenePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
