//! forgia-scene
//!
//! Scene loader + map_switch + DespawnOnExit
//!
//! Category : world

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaScenePlugin).
pub struct ForgiaScenePlugin;

impl Plugin for ForgiaScenePlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
