# forgia-pp-color-grading

> LUT-based color grading per game mode

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_color_grading::ForgiaPpColorGradingPlugin;
app.add_plugins(ForgiaPpColorGradingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
