# forgia-render-env-map

> Environment cubemap reflection

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_env_map::ForgiaRenderEnvMapPlugin;
app.add_plugins(ForgiaRenderEnvMapPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
