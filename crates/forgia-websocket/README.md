# forgia-websocket

> Port websocket_plugin (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_websocket::ForgiaWebsocketPlugin;
app.add_plugins(ForgiaWebsocketPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
