# forgia-telemetry-funnel

> Conversion funnel telemetry

**Category** : `meta` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_telemetry_funnel::ForgiaTelemetryFunnelPlugin;
app.add_plugins(ForgiaTelemetryFunnelPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
