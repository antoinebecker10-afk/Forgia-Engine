# forgia-input-recording

> Input recording for replay/QA

**Category** : `input` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_input_recording::ForgiaInputRecordingPlugin;
app.add_plugins(ForgiaInputRecordingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
