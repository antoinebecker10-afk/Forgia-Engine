# forgia-net-rollback

> Rollback netcode helper

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_rollback::ForgiaNetRollbackPlugin;
app.add_plugins(ForgiaNetRollbackPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
