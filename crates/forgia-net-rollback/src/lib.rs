//! forgia-net-rollback
//!
//! Rollback netcode helper
//!
//! Category : network

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaNetRollbackPlugin).
pub struct ForgiaNetRollbackPlugin;

impl Plugin for ForgiaNetRollbackPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
