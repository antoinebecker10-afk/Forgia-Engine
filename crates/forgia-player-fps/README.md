# forgia-player-fps

> FPS 1P controller (kinematic char ctrl)

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_player_fps::ForgiaPlayerFpsPlugin;
app.add_plugins(ForgiaPlayerFpsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
