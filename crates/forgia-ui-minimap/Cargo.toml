[package]
name = "forgia-ui-minimap"
version = "0.1.0"
edition = "2021"
description = "Minimap with cached recalc (L8 lock)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
bevy_egui = { workspace = true }
