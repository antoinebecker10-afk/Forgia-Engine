# forgia-ui-minimap

> Minimap with cached recalc (L8 lock)

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_minimap::ForgiaUiMinimapPlugin;
app.add_plugins(ForgiaUiMinimapPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
