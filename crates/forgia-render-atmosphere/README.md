# forgia-render-atmosphere

> Atmospheric scattering

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_atmosphere::ForgiaRenderAtmospherePlugin;
app.add_plugins(ForgiaRenderAtmospherePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
