# forgia-app-state

> States/SubStates wrapper + transitions safe

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_app_state::ForgiaAppStatePlugin;
app.add_plugins(ForgiaAppStatePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
