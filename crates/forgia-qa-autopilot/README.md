# forgia-qa-autopilot

> Autopilot AI bot for QA runs

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_autopilot::ForgiaQaAutopilotPlugin;
app.add_plugins(ForgiaQaAutopilotPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
