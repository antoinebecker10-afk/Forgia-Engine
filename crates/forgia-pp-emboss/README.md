# forgia-pp-emboss

> Emboss filter

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_emboss::ForgiaPpEmbossPlugin;
app.add_plugins(ForgiaPpEmbossPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
