# forgia-input-azerty-qwerty

> AZERTY/QWERTY layout swap

**Category** : `input` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_input_azerty_qwerty::ForgiaInputAzertyQwertyPlugin;
app.add_plugins(ForgiaInputAzertyQwertyPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
