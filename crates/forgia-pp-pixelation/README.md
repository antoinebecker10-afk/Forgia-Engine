# forgia-pp-pixelation

> Square pixelation

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_pixelation::ForgiaPpPixelationPlugin;
app.add_plugins(ForgiaPpPixelationPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
