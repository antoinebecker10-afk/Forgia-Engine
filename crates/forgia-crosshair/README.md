# forgia-crosshair

> Dynamic crosshair (spread, hit confirm)

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_crosshair::ForgiaCrosshairPlugin;
app.add_plugins(ForgiaCrosshairPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
