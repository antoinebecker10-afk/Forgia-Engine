# forgia-audio-voicelines

> Voicelines registry (living_weapons V1)

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_voicelines::ForgiaAudioVoicelinesPlugin;
app.add_plugins(ForgiaAudioVoicelinesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
