# forgia-audio-core

> bevy_kira_audio wrapper + channels

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_core::ForgiaAudioCorePlugin;
app.add_plugins(ForgiaAudioCorePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
