# forgia-pp-tilt-shift

> Tilt-shift miniature effect

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_tilt_shift::ForgiaPpTiltShiftPlugin;
app.add_plugins(ForgiaPpTiltShiftPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
