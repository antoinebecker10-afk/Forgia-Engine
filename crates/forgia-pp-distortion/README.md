# forgia-pp-distortion

> Generic UV distortion

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_distortion::ForgiaPpDistortionPlugin;
app.add_plugins(ForgiaPpDistortionPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
