# forgia-foliage

> Vegetation placement (grass, trees) genome-driven

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_foliage::ForgiaFoliagePlugin;
app.add_plugins(ForgiaFoliagePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
