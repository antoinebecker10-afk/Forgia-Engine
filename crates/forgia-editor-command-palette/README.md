# forgia-editor-command-palette

> Cmd+P fuzzy palette

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_command_palette::ForgiaEditorCommandPalettePlugin;
app.add_plugins(ForgiaEditorCommandPalettePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
