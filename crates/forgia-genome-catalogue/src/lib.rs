//! forgia-genome-catalogue
//!
//! Catalogue of all genomes (weapon, biome, enemy)
//!
//! Category : genome

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaGenomeCataloguePlugin).
pub struct ForgiaGenomeCataloguePlugin;

impl Plugin for ForgiaGenomeCataloguePlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
