# forgia-genome-catalogue

> Catalogue of all genomes (weapon, biome, enemy)

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_catalogue::ForgiaGenomeCataloguePlugin;
app.add_plugins(ForgiaGenomeCataloguePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
