# forgia-pp-auto-exposure

> Auto-exposure (luminance adaptation)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_auto_exposure::ForgiaPpAutoExposurePlugin;
app.add_plugins(ForgiaPpAutoExposurePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
