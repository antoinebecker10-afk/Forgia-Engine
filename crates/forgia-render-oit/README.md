# forgia-render-oit

> Order-independent transparency

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_oit::ForgiaRenderOitPlugin;
app.add_plugins(ForgiaRenderOitPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
