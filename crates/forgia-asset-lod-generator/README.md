# forgia-asset-lod-generator

> LOD auto-generation

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_asset_lod_generator::ForgiaAssetLodGeneratorPlugin;
app.add_plugins(ForgiaAssetLodGeneratorPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
