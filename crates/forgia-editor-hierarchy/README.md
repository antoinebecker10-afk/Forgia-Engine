# forgia-editor-hierarchy

> Scene hierarchy tree panel

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_hierarchy::ForgiaEditorHierarchyPlugin;
app.add_plugins(ForgiaEditorHierarchyPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
