# forgia-qa-replay

> Input replay for regression testing

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_replay::ForgiaQaReplayPlugin;
app.add_plugins(ForgiaQaReplayPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
