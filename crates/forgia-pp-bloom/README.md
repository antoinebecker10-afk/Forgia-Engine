# forgia-pp-bloom

> Tunable bloom (extends Bevy default)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_bloom::ForgiaPpBloomPlugin;
app.add_plugins(ForgiaPpBloomPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
