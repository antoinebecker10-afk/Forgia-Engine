# forgia-hitmarker

> Hit confirmation visual + audio

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_hitmarker::ForgiaHitmarkerPlugin;
app.add_plugins(ForgiaHitmarkerPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
