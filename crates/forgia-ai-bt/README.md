# forgia-ai-bt

> Behavior trees

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_bt::ForgiaAiBtPlugin;
app.add_plugins(ForgiaAiBtPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
