# forgia-spline

> Splines/paths/rails for AI/cinematics

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_spline::ForgiaSplinePlugin;
app.add_plugins(ForgiaSplinePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
