[package]
name = "forgia-debug"
version = "0.1.0"
edition = "2021"
description = "Forgia dev-loop debug UX — F-keys catalog + F3 live overlay + hot-reload. Distinct from forgia-qa-* (automation)."

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
bevy_egui = "0.39.1"
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
