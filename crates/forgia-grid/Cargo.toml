[package]
name = "forgia-grid"
version = "0.1.0"
edition = "2021"
description = "Debug + builder grid overlay"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
