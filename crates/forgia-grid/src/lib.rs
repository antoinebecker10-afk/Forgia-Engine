//! forgia-grid
//!
//! Debug + builder grid overlay
//!
//! Category : world

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaGridPlugin).
pub struct ForgiaGridPlugin;

impl Plugin for ForgiaGridPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
