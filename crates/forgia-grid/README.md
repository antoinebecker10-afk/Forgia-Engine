# forgia-grid

> Debug + builder grid overlay

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_grid::ForgiaGridPlugin;
app.add_plugins(ForgiaGridPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
