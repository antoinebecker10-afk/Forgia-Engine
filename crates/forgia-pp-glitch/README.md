# forgia-pp-glitch

> Digital glitch artifacts

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_glitch::ForgiaPpGlitchPlugin;
app.add_plugins(ForgiaPpGlitchPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
