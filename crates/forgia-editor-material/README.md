# forgia-editor-material

> Material node graph editor

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_material::ForgiaEditorMaterialPlugin;
app.add_plugins(ForgiaEditorMaterialPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
