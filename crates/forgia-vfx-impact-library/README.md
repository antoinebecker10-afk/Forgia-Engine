# forgia-vfx-impact-library

> Impact preset library per surface type

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_vfx_impact_library::ForgiaVfxImpactLibraryPlugin;
app.add_plugins(ForgiaVfxImpactLibraryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
