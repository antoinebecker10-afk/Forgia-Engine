# forgia-assets-bundle

> Asset bundle pack (zstd)

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_assets_bundle::ForgiaAssetsBundlePlugin;
app.add_plugins(ForgiaAssetsBundlePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
