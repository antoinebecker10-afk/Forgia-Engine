//! forgia-animation-mixamo
//!
//! Mixamo rig + clip registry (player_anim_*)
//!
//! Category : animation

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaAnimationMixamoPlugin).
pub struct ForgiaAnimationMixamoPlugin;

impl Plugin for ForgiaAnimationMixamoPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
