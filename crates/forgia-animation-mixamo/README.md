# forgia-animation-mixamo

> Mixamo rig + clip registry (player_anim_*)

**Category** : `animation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_animation_mixamo::ForgiaAnimationMixamoPlugin;
app.add_plugins(ForgiaAnimationMixamoPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
