[package]
name = "forgia-animation-mixamo"
version = "0.1.0"
edition = "2021"
description = "Mixamo rig + clip registry (player_anim_*)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
