# forgia-silk

> Port bevy_silk cloth (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_silk::ForgiaSilkPlugin;
app.add_plugins(ForgiaSilkPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
