# forgia-weapon-hitscan

> Instant raycast weapon with falloff

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_weapon_hitscan::ForgiaWeaponHitscanPlugin;
app.add_plugins(ForgiaWeaponHitscanPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
