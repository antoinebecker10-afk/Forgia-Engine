# forgia-cloth

> Cloth simulation (capes, banners)

**Category** : `physics` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_cloth::ForgiaClothPlugin;
app.add_plugins(ForgiaClothPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
