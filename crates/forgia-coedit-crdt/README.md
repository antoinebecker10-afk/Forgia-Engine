# forgia-coedit-crdt

> Automerge JSON-CRDT co-edit (Forgia unique)

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_coedit_crdt::ForgiaCoeditCrdtPlugin;
app.add_plugins(ForgiaCoeditCrdtPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
