# forgia-ai-state-machine

> FSM helper for AI

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_state_machine::ForgiaAiStateMachinePlugin;
app.add_plugins(ForgiaAiStateMachinePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
