# forgia-render-skybox

> Skybox cubemap with day/night cycle

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_skybox::ForgiaRenderSkyboxPlugin;
app.add_plugins(ForgiaRenderSkyboxPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
