//! forgia-render-skybox
//!
//! Skybox cubemap with day/night cycle
//!
//! Category : render

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaRenderSkyboxPlugin).
pub struct ForgiaRenderSkyboxPlugin;

impl Plugin for ForgiaRenderSkyboxPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
