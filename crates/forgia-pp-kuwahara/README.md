# forgia-pp-kuwahara

> Kuwahara painterly filter

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_kuwahara::ForgiaPpKuwaharaPlugin;
app.add_plugins(ForgiaPpKuwaharaPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
