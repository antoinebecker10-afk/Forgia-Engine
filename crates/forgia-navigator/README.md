# forgia-navigator

> Port vleue_navigator (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_navigator::ForgiaNavigatorPlugin;
app.add_plugins(ForgiaNavigatorPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
