# forgia-ui-objectives

> Quest objectives panel

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_objectives::ForgiaUiObjectivesPlugin;
app.add_plugins(ForgiaUiObjectivesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
