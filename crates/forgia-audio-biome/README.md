# forgia-audio-biome

> Biome ambient audio (Forgia)

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_biome::ForgiaAudioBiomePlugin;
app.add_plugins(ForgiaAudioBiomePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
