# forgia-asset-atlas-packer

> Texture atlas packer

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_asset_atlas_packer::ForgiaAssetAtlasPackerPlugin;
app.add_plugins(ForgiaAssetAtlasPackerPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
