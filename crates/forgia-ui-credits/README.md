# forgia-ui-credits

> Credits roll

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_credits::ForgiaUiCreditsPlugin;
app.add_plugins(ForgiaUiCreditsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
