[package]
name = "forgia-pp-palette-quantize"
version = "0.1.0"
edition = "2021"
description = "Palette quantization (LUT lookup)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
