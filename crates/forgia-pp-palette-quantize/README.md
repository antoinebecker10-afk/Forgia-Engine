# forgia-pp-palette-quantize

> Palette quantization (LUT lookup)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_palette_quantize::ForgiaPpPaletteQuantizePlugin;
app.add_plugins(ForgiaPpPaletteQuantizePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
