# forgia-render-rt

> Raytracing (optional, hardware)

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_rt::ForgiaRenderRtPlugin;
app.add_plugins(ForgiaRenderRtPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
