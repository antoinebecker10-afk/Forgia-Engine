[package]
name = "forgia-mode-fps-arena"
version = "0.1.0"
edition = "2021"
description = "FPS arena mode plugin (V1)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
