# forgia-mode-fps-arena

> FPS arena mode plugin (V1)

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_fps_arena::ForgiaModeFpsArenaPlugin;
app.add_plugins(ForgiaModeFpsArenaPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
