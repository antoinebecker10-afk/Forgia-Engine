//! forgia-mode-fps-arena
//!
//! FPS arena mode plugin (V1)
//!
//! Category : mode

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaModeFpsArenaPlugin).
pub struct ForgiaModeFpsArenaPlugin;

impl Plugin for ForgiaModeFpsArenaPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
