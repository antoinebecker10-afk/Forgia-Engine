# forgia-pp-radial-blur

> Radial blur (zoom or rotation)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_radial_blur::ForgiaPpRadialBlurPlugin;
app.add_plugins(ForgiaPpRadialBlurPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
