# forgia-pp-ascii

> ASCII art post-process

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_ascii::ForgiaPpAsciiPlugin;
app.add_plugins(ForgiaPpAsciiPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
