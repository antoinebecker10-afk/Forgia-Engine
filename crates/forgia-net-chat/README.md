# forgia-net-chat

> In-game chat (replicated)

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_chat::ForgiaNetChatPlugin;
app.add_plugins(ForgiaNetChatPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
