//! forgia-net-chat
//!
//! In-game chat (replicated)
//!
//! Category : network

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaNetChatPlugin).
pub struct ForgiaNetChatPlugin;

impl Plugin for ForgiaNetChatPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
