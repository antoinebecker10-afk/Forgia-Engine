# forgia-player-vehicle-control

> Vehicle control wrapper

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_player_vehicle_control::ForgiaPlayerVehicleControlPlugin;
app.add_plugins(ForgiaPlayerVehicleControlPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
