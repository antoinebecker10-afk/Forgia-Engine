//! forgia-player-vehicle-control
//!
//! Vehicle control wrapper
//!
//! Category : player

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaPlayerVehicleControlPlugin).
pub struct ForgiaPlayerVehicleControlPlugin;

impl Plugin for ForgiaPlayerVehicleControlPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
