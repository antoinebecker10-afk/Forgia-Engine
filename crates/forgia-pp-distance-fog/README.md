# forgia-pp-distance-fog

> Depth-based fog

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_distance_fog::ForgiaPpDistanceFogPlugin;
app.add_plugins(ForgiaPpDistanceFogPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
