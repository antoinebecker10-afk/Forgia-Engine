# forgia-pp-invert

> Color invert

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_invert::ForgiaPpInvertPlugin;
app.add_plugins(ForgiaPpInvertPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
