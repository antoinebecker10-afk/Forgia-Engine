# forgia-gauge

> Port bevy_gauge (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_gauge::ForgiaGaugePlugin;
app.add_plugins(ForgiaGaugePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
