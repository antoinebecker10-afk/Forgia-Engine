# forgia-genome-balance

> Balance audit + drift detection

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_balance::ForgiaGenomeBalancePlugin;
app.add_plugins(ForgiaGenomeBalancePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
