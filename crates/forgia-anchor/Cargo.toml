[package]
name = "forgia-anchor"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Generic anchor-point system (PlayerSpawn / PoiSlot / Landmark / BossPad / Teleporter / LootZone). Mode-agnostic, used by Roguelite/Arena/RPG. Story-483."

[lints]
workspace = true

[dependencies]
web-time = { workspace = true }
bevy = { workspace = true }
forgia-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
