# forgia-editor-inspector

> Reflect-based component inspector

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_inspector::ForgiaEditorInspectorPlugin;
app.add_plugins(ForgiaEditorInspectorPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
