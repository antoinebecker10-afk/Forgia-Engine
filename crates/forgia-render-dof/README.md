# forgia-render-dof

> Depth of field circular bokeh

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_dof::ForgiaRenderDofPlugin;
app.add_plugins(ForgiaRenderDofPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
