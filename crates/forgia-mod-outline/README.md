# forgia-mod-outline

> Port bevy_mod_outline (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mod_outline::ForgiaModOutlinePlugin;
app.add_plugins(ForgiaModOutlinePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
