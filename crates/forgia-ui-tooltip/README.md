# forgia-ui-tooltip

> Tooltip on hover

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_tooltip::ForgiaUiTooltipPlugin;
app.add_plugins(ForgiaUiTooltipPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
