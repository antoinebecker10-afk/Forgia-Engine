# forgia-mode-platformer

> Platformer mode plugin

**Category** : `mode` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_mode_platformer::ForgiaModePlatformerPlugin;
app.add_plugins(ForgiaModePlatformerPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
