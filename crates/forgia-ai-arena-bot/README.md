# forgia-ai-arena-bot

> Arena FPS bot (V1 respawn + nav)

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_arena_bot::ForgiaAiArenaBotPlugin;
app.add_plugins(ForgiaAiArenaBotPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
