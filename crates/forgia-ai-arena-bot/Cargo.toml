[package]
name = "forgia-ai-arena-bot"
version = "0.1.0"
edition = "2021"
description = "Arena FPS bot — simple state machine (Idle/Chase/Attack) + respawn"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
forgia-damage = { path = "../forgia-damage" }
bevy_rapier3d = { workspace = true }
