# forgia-pp-sketch

> Pencil sketch hatch lines

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_sketch::ForgiaPpSketchPlugin;
app.add_plugins(ForgiaPpSketchPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
