# forgia-editor-foliage

> Foliage placement brush

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_foliage::ForgiaEditorFoliagePlugin;
app.add_plugins(ForgiaEditorFoliagePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
