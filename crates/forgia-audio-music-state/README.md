# forgia-audio-music-state

> Adaptive music state machine

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_music_state::ForgiaAudioMusicStatePlugin;
app.add_plugins(ForgiaAudioMusicStatePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
