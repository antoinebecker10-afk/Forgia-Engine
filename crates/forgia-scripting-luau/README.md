# forgia-scripting-luau

> bevy_mod_scripting Luau wrapper

**Category** : `scripting` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_scripting_luau::ForgiaScriptingLuauPlugin;
app.add_plugins(ForgiaScriptingLuauPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
