# forgia-asset-cdn

> CDN-fetched asset registry (V2 marketplace)

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_asset_cdn::ForgiaAssetCdnPlugin;
app.add_plugins(ForgiaAssetCdnPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
