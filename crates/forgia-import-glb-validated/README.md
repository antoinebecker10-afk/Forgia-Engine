# forgia-import-glb-validated

> GLB import with PBR validation

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_import_glb_validated::ForgiaImportGlbValidatedPlugin;
app.add_plugins(ForgiaImportGlbValidatedPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
