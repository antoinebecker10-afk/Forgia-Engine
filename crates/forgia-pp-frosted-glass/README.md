# forgia-pp-frosted-glass

> Frosted glass blur

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_frosted_glass::ForgiaPpFrostedGlassPlugin;
app.add_plugins(ForgiaPpFrostedGlassPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
