# forgia-dialogue

> Dialogue trees (living_weapons V1 voiced)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_dialogue::ForgiaDialoguePlugin;
app.add_plugins(ForgiaDialoguePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
