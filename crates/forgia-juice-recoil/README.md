# forgia-juice-recoil

> Apex-style camera recoil event

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_juice_recoil::ForgiaJuiceRecoilPlugin;
app.add_plugins(ForgiaJuiceRecoilPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
