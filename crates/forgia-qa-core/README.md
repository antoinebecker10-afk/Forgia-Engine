# forgia-qa-core

> QA pipeline core

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_core::ForgiaQaCorePlugin;
app.add_plugins(ForgiaQaCorePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
