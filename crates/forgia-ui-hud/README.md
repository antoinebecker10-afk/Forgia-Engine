# forgia-ui-hud

> HP/ammo/crosshair/score HUD

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_hud::ForgiaUiHudPlugin;
app.add_plugins(ForgiaUiHudPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
