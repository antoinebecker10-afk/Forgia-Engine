# forgia-ragdoll

> Ragdoll on death

**Category** : `physics` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ragdoll::ForgiaRagdollPlugin;
app.add_plugins(ForgiaRagdollPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
