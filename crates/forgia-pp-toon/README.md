# forgia-pp-toon

> Toon/cel-shading band quantization

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_toon::ForgiaPpToonPlugin;
app.add_plugins(ForgiaPpToonPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
