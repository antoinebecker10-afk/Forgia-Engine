# forgia-weapon-beam

> Continuous beam weapon

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_weapon_beam::ForgiaWeaponBeamPlugin;
app.add_plugins(ForgiaWeaponBeamPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
