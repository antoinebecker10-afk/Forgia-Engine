# forgia-pp-grayscale

> Grayscale (luma-weighted)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_grayscale::ForgiaPpGrayscalePlugin;
app.add_plugins(ForgiaPpGrayscalePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
