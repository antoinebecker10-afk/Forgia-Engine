# forgia-quests

> Quest system (objectives, rewards)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_quests::ForgiaQuestsPlugin;
app.add_plugins(ForgiaQuestsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
