# forgia-juice-hit-stop

> Hit-stop time pause genome-driven

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_juice_hit_stop::ForgiaJuiceHitStopPlugin;
app.add_plugins(ForgiaJuiceHitStopPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
