# forgia-rng

> Seeded deterministic RNG resource (bevy_rand wrapper)

**Category** : `foundation` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_rng::ForgiaRngPlugin;
app.add_plugins(ForgiaRngPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
