# forgia-pp-vignette

> Edge darkening vignette

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_vignette::ForgiaPpVignettePlugin;
app.add_plugins(ForgiaPpVignettePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
