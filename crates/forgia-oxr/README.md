# forgia-oxr

> Port bevy_oxr VR (renzora vendored)

**Category** : `port` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_oxr::ForgiaOxrPlugin;
app.add_plugins(ForgiaOxrPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
