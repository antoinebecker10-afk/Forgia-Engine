# forgia-editor-asset-browser

> Asset browser thumbnails

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_asset_browser::ForgiaEditorAssetBrowserPlugin;
app.add_plugins(ForgiaEditorAssetBrowserPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
