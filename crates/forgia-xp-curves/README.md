# forgia-xp-curves

> XP/level curves genome-driven (Forgia unique)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_xp_curves::ForgiaXpCurvesPlugin;
app.add_plugins(ForgiaXpCurvesPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
