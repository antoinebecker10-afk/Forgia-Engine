# forgia-pp-oil-painting

> Oil painting stylise

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_oil_painting::ForgiaPpOilPaintingPlugin;
app.add_plugins(ForgiaPpOilPaintingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
