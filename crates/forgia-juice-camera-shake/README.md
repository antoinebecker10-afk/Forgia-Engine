# forgia-juice-camera-shake

> Camera shake (trauma-based)

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_juice_camera_shake::ForgiaJuiceCameraShakePlugin;
app.add_plugins(ForgiaJuiceCameraShakePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
