# forgia-armor

> Armor mitigation layer

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_armor::ForgiaArmorPlugin;
app.add_plugins(ForgiaArmorPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
