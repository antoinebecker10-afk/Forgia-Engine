# forgia-editor-blueprint

> Visual scripting graph editor

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_blueprint::ForgiaEditorBlueprintPlugin;
app.add_plugins(ForgiaEditorBlueprintPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
