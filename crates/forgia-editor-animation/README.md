# forgia-editor-animation

> Animation timeline editor

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_animation::ForgiaEditorAnimationPlugin;
app.add_plugins(ForgiaEditorAnimationPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
