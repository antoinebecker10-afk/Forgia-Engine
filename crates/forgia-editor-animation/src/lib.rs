//! forgia-editor-animation
//!
//! Animation timeline editor
//!
//! Category : editor

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaEditorAnimationPlugin).
pub struct ForgiaEditorAnimationPlugin;

impl Plugin for ForgiaEditorAnimationPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
