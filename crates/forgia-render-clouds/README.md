# forgia-render-clouds

> Volumetric clouds

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_clouds::ForgiaRenderCloudsPlugin;
app.add_plugins(ForgiaRenderCloudsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
