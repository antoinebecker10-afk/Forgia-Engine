# forgia-editor-particle

> Hanabi particle editor

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_particle::ForgiaEditorParticlePlugin;
app.add_plugins(ForgiaEditorParticlePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
