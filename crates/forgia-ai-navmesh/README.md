# forgia-ai-navmesh

> Navmesh wrapper (bevy_landmass)

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_navmesh::ForgiaAiNavmeshPlugin;
app.add_plugins(ForgiaAiNavmeshPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
