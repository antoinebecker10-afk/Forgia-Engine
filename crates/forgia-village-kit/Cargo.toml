[package]
name = "forgia-village-kit"
version = "0.1.0"
edition = "2021"
description = "Forgia V2 — Village kit vocabulary (TOML types + KitResolver). Pure data, no Bevy."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
