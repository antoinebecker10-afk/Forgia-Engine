# forgia-crafting

> Recipe + craft system genome-driven

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_crafting::ForgiaCraftingPlugin;
app.add_plugins(ForgiaCraftingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
