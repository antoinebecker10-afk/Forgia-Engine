# forgia-weapon-charged

> Charged shot (hold to charge)

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_weapon_charged::ForgiaWeaponChargedPlugin;
app.add_plugins(ForgiaWeaponChargedPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
