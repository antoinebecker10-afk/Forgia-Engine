# forgia-script-sandbox

> Sandboxed script exec (user-generated)

**Category** : `scripting` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_script_sandbox::ForgiaScriptSandboxPlugin;
app.add_plugins(ForgiaScriptSandboxPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
