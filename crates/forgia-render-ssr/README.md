# forgia-render-ssr

> Screen-space reflections

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_ssr::ForgiaRenderSsrPlugin;
app.add_plugins(ForgiaRenderSsrPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
