# forgia-pp-outline

> Edge outline (sobel-derived) for cel-shading

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_outline::ForgiaPpOutlinePlugin;
app.add_plugins(ForgiaPpOutlinePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
