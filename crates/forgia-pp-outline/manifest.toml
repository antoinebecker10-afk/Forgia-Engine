# Forgia capability manifest — consumed by AI assembly agent.
# Schema v1.

name = "forgia-pp-outline"
category = "postprocess"
intent = "Sobel edge outline (cel-shading companion to forgia-pp-toon)"
version = "0.1.0"
status = "wip"   # shader + settings ready ; render-graph node TODO

# Genes exposed via TOML hot-reload.
genes = [
    { name = "thickness", range = [0.5, 3.0],  default = 1.0,  doc = "Texel sampling radius" },
    { name = "threshold", range = [0.05, 0.5], default = 0.15, doc = "Gradient magnitude cutoff" },
    { name = "strength",  range = [0.0, 1.0],  default = 1.0,  doc = "Blend factor" },
]

emits = []
consumes = ["OutlineSettings (Component on Camera entity)"]
sensor = ""

shader = "shaders/post_process/outline.wgsl"

depends = ["forgia-core"]
