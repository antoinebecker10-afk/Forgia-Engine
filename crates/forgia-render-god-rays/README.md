# forgia-render-god-rays

> Volumetric god rays

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_god_rays::ForgiaRenderGodRaysPlugin;
app.add_plugins(ForgiaRenderGodRaysPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
