# forgia-ai-goap

> GOAP (Goal-Oriented Action Planning)

**Category** : `ai` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ai_goap::ForgiaAiGoapPlugin;
app.add_plugins(ForgiaAiGoapPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
