# forgia-ribbons

> Ribbon trails (smoke, fire)

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ribbons::ForgiaRibbonsPlugin;
app.add_plugins(ForgiaRibbonsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
