# forgia-juice-fov-punch

> FOV punch on shoot/hit

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_juice_fov_punch::ForgiaJuiceFovPunchPlugin;
app.add_plugins(ForgiaJuiceFovPunchPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
