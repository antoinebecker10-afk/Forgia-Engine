# forgia-level-presets

> Level templates (Arena/RPG/Race/Survival)

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_level_presets::ForgiaLevelPresetsPlugin;
app.add_plugins(ForgiaLevelPresetsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
