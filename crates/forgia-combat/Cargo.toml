[package]
name = "forgia-combat"
version = "0.1.0"
edition = "2021"
description = "Forgia V2 — Gunfeel V5-F partagé FPS/RPG : weapons, viewmodel, hit-stop, recoil, hitmarker, damage falloff."

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
bevy_rapier3d = { workspace = true }
leafwing-input-manager = { workspace = true }
forgia-core = { workspace = true }
forgia-damage = { workspace = true }
forgia-juice-lib = { workspace = true }
# Keystone 0.1b (story-634) — RNG déterministe (xoshiro256++ pur) pour CombatRng.
forgia-rng = { workspace = true }
# forgia-assets = { workspace = true }    # TODO: uncomment when GameAssets V2 ported
# forgia-player = { workspace = true }    # TODO: uncomment when Player/FpsCamera/GoblinGuard ported
# forgia-effects = { workspace = true }   # TODO: circular dep risk — combat_juice events only; re-evaluate
