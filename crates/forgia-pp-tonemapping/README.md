# forgia-pp-tonemapping

> Tonemapping presets (ACES, Reinhard, etc.)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_tonemapping::ForgiaPpTonemappingPlugin;
app.add_plugins(ForgiaPpTonemappingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
