# forgia-render-forward-decal

> Forward decal rendering

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_forward_decal::ForgiaRenderForwardDecalPlugin;
app.add_plugins(ForgiaRenderForwardDecalPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
