# forgia-vfx-decals

> Bullet impact decals

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_vfx_decals::ForgiaVfxDecalsPlugin;
app.add_plugins(ForgiaVfxDecalsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
