# forgia-script-hot-reload

> Hot-reload Luau on file change

**Category** : `scripting` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_script_hot_reload::ForgiaScriptHotReloadPlugin;
app.add_plugins(ForgiaScriptHotReloadPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
