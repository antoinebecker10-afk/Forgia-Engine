# forgia-qa-kg

> Knowledge graph for bug patterns

**Category** : `qa` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_qa_kg::ForgiaQaKgPlugin;
app.add_plugins(ForgiaQaKgPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
