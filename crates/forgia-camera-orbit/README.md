# forgia-camera-orbit

> Orbit camera (3P RPG, with collision)

**Category** : `player` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_camera_orbit::ForgiaCameraOrbitPlugin;
app.add_plugins(ForgiaCameraOrbitPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
