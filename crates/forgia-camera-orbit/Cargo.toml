[package]
name = "forgia-camera-orbit"
version = "0.1.0"
edition = "2021"
description = "Orbit camera (3P RPG, with collision)"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
