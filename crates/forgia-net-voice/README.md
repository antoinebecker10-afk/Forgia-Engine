# forgia-net-voice

> Voice chat (opus)

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_voice::ForgiaNetVoicePlugin;
app.add_plugins(ForgiaNetVoicePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
