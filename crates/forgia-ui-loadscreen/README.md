# forgia-ui-loadscreen

> Loading screen with progress

**Category** : `ui` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_ui_loadscreen::ForgiaUiLoadscreenPlugin;
app.add_plugins(ForgiaUiLoadscreenPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
