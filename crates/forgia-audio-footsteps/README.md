# forgia-audio-footsteps

> Footsteps per surface material

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_footsteps::ForgiaAudioFootstepsPlugin;
app.add_plugins(ForgiaAudioFootstepsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
