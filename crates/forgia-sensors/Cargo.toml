[package]
name = "forgia-sensors"
version = "0.1.0"
edition = "2021"
description = "Forgia V2 — Stack observability : 12 sensors max (vs 95 V1), health monitor, lag detector, watchdog OS thread."

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
forgia-core = { workspace = true }
