# forgia-render-pcss

> Percentage-closer soft shadows

**Category** : `render` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_render_pcss::ForgiaRenderPcssPlugin;
app.add_plugins(ForgiaRenderPcssPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
