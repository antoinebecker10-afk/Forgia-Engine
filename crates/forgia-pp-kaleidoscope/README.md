# forgia-pp-kaleidoscope

> Kaleidoscope mirror

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_kaleidoscope::ForgiaPpKaleidoscopePlugin;
app.add_plugins(ForgiaPpKaleidoscopePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
