# forgia-editor-viewport

> Editor viewport camera + grid

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_viewport::ForgiaEditorViewportPlugin;
app.add_plugins(ForgiaEditorViewportPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
