[package]
name = "forgia-editor-viewport"
version = "0.1.0"
edition = "2021"
description = "Editor viewport camera + grid"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
forgia-core = { workspace = true }
