//! forgia-editor-viewport
//!
//! Editor viewport camera + grid
//!
//! Category : editor

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaEditorViewportPlugin).
pub struct ForgiaEditorViewportPlugin;

impl Plugin for ForgiaEditorViewportPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
