# forgia-shape-library

> Primitive shapes library (cube/sphere/capsule)

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_shape_library::ForgiaShapeLibraryPlugin;
app.add_plugins(ForgiaShapeLibraryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
