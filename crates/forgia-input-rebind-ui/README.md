# forgia-input-rebind-ui

> Rebind UI panel

**Category** : `input` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_input_rebind_ui::ForgiaInputRebindUiPlugin;
app.add_plugins(ForgiaInputRebindUiPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
