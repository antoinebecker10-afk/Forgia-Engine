# forgia-pp-swirl

> Swirl distortion

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_swirl::ForgiaPpSwirlPlugin;
app.add_plugins(ForgiaPpSwirlPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
