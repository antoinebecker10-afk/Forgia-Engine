# forgia-pp-sobel-edge

> Sobel edge detection

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_sobel_edge::ForgiaPpSobelEdgePlugin;
app.add_plugins(ForgiaPpSobelEdgePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
