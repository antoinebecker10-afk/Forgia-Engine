# forgia-pp-dream

> Dream-like soft glow

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_dream::ForgiaPpDreamPlugin;
app.add_plugins(ForgiaPpDreamPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
