# forgia-vfx-tracers

> Bullet tracers cached mesh (Forgia gunfeel V5)

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_vfx_tracers::ForgiaVfxTracersPlugin;
app.add_plugins(ForgiaVfxTracersPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
