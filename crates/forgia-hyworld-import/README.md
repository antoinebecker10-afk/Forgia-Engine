# forgia-hyworld-import

> HyWorld .hyworld importer

**Category** : `assets` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_hyworld_import::ForgiaHyworldImportPlugin;
app.add_plugins(ForgiaHyworldImportPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
