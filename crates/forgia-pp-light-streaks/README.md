# forgia-pp-light-streaks

> Light streak (anamorphic)

**Category** : `postprocess` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_pp_light_streaks::ForgiaPpLightStreaksPlugin;
app.add_plugins(ForgiaPpLightStreaksPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
