# forgia-editor-core

> Editor app shell (Play/Build/Edit modes)

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_core::ForgiaEditorCorePlugin;
app.add_plugins(ForgiaEditorCorePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
