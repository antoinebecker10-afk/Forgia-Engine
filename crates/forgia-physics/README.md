# forgia-physics

> Rapier wrapper + collision groups G1-G5

**Category** : `physics` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_physics::ForgiaPhysicsPlugin;
app.add_plugins(ForgiaPhysicsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
