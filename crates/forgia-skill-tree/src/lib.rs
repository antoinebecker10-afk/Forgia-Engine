//! forgia-skill-tree
//!
//! Skill tree (nodes, prereqs, allocation)
//!
//! Category : gameplay

use bevy::prelude::*;

/// Plugin Bevy. Add to App via pp.add_plugins(ForgiaSkillTreePlugin).
pub struct ForgiaSkillTreePlugin;

impl Plugin for ForgiaSkillTreePlugin {
    fn build(&self, _app: &mut App) {
        // TODO: implement
    }
}
