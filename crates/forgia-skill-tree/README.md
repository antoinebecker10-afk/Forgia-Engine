# forgia-skill-tree

> Skill tree (nodes, prereqs, allocation)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_skill_tree::ForgiaSkillTreePlugin;
app.add_plugins(ForgiaSkillTreePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
