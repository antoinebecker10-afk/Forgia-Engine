# forgia-editor-gizmo

> Translate/rotate/scale gizmos

**Category** : `editor` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_editor_gizmo::ForgiaEditorGizmoPlugin;
app.add_plugins(ForgiaEditorGizmoPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
