# forgia-prefab

> Blueprint/prefab system for AI assembly

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_prefab::ForgiaPrefabPlugin;
app.add_plugins(ForgiaPrefabPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
