# forgia-genome-validator

> Schema validation + missing-gene detection

**Category** : `genome` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_genome_validator::ForgiaGenomeValidatorPlugin;
app.add_plugins(ForgiaGenomeValidatorPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
