# forgia-underwater

> Player swim + underwater PP gate (sea_level)

**Category** : `world` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_underwater::ForgiaUnderwaterPlugin;
app.add_plugins(ForgiaUnderwaterPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
