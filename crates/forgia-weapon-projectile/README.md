# forgia-weapon-projectile

> Projectile weapon (rockets, grenades)

**Category** : `combat` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_weapon_projectile::ForgiaWeaponProjectilePlugin;
app.add_plugins(ForgiaWeaponProjectilePlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
