# forgia-net-lightyear

> lightyear wrapper + replication channels

**Category** : `network` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_net_lightyear::ForgiaNetLightyearPlugin;
app.add_plugins(ForgiaNetLightyearPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
