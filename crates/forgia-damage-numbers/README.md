# forgia-damage-numbers

> Floating damage numbers (3D world-space)

**Category** : `effects` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_damage_numbers::ForgiaDamageNumbersPlugin;
app.add_plugins(ForgiaDamageNumbersPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
