# forgia-inventory

> Inventory grid 80 slots (LOCK-INV-1)

**Category** : `gameplay` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_inventory::ForgiaInventoryPlugin;
app.add_plugins(ForgiaInventoryPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
