# forgia-audio-ducking

> Audio ducking (music behind voice)

**Category** : `audio` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_audio_ducking::ForgiaAudioDuckingPlugin;
app.add_plugins(ForgiaAudioDuckingPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
