# forgia-script-api-bindings

> ECS bindings exposed to Luau

**Category** : `scripting` · **Status** : `stub`

## Usage (AI assembly)

```rust
use forgia_script_api_bindings::ForgiaScriptApiBindingsPlugin;
app.add_plugins(ForgiaScriptApiBindingsPlugin);
```

## Manifest

See [`manifest.toml`](./manifest.toml) for AI-callable contract (genes, emits, consumes, sensor).
