[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Forgia V2 — Automation tasks : check-orphans, schedule-dump, baseline E1/E2."

[lints]
workspace = true

[dependencies]
serde_json = { workspace = true }
toml = { workspace = true }
