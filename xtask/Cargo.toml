[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Forgia V2 — Automation tasks : check-orphans, schedule-dump, baseline E1/E2."

[lints]
workspace = true

[dependencies]
forgia-pcg-core = { workspace = true }
serde_json = { workspace = true }
# SHA-256 du publisher PCG : validate-pcg recalcule le hash des manifests
# référencés par registry.lock.toml et refuse toute divergence.
sha2 = "0.10"
toml = { workspace = true }
