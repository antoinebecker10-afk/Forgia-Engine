# Audit Wiring — Forgia Rewrite V2

**Date** : 2026-05-27
**Workspace** : `C:/Users/Antoi/Desktop/Forgia Rewrite/`
**Méthode** : 3 agents // — Plugin wiring (Bevy `add_plugins`), Cargo dependency graph (`cargo metadata`), pub items consumer analysis.
**Scope** : 62 crates Cargo members, 49 Plugins définis.

---

## Verdict global

| Axe | Total | Sains | Problèmes |
|---|---|---|---|
| Cargo deps (libs) | 58 | 55 (95%) | **3 orphelins** |
| Bevy Plugins | 49 | 35 (71%) | **14 orphelins runtime + 2 double-add panic-risk** |
| Pub items lib.rs | 59 | 54 (92%) | **3 fantômes purs + 2 bin-only mal classés** |

**Constat clé** : Le gap **wired Cargo ≠ wired Bevy runtime** est le risque dominant. 11 crates passent l'audit Cargo (consumers déclarés) mais leurs Plugins ne sont jamais `add_plugins(...)` — leurs systèmes ne tournent jamais runtime.

---

## 1. Crates 100% orphelines (Cargo + Plugin + pub items)

À supprimer ou re-câbler explicitement :

| Crate | Cargo incoming | Plugin wired | pub items consumers |
|---|---|---|---|
| **forgia-weapon-hitscan** | 0 | 0 | 0 |
| **forgia-postprocess** | 0 | 0 (2 sub-plugins toon/outline orphelins) | 0 |
| **forgia-spline** | 1 (Cargo.toml seul, 0 `use` Rust) | n/a | 0 |

**Action recommandée** : DELETE ou story de re-wiring documentée. `forgia-postprocess` est issue de la fusion story-513 (45 `forgia-pp-*` → 1) — fusion livrée mais re-exports jamais réintégrés runtime.

---

## 2. Plugins orphelins (crate wirée Cargo, Plugin pas instancié)

14 plugins définis sans `add_plugins(...)` consumer :

### Crates avec types utilisés mais Plugin mort
- `ForgiaGenomeCorePlugin` — `forgia-genome-core` (lib utilisée pour types/asset loader)
- `ForgiaManifestPlugin` — `forgia-genome-core/manifest` (fusion 2026-05-26)
- `ForgiaPackRegistryPlugin` — `forgia-asset-registry/pack_registry` (le parent ForgiaAssetRegistryPlugin ne charge pas le sub)
- `ForgiaVfxTracersPlugin` — `forgia-effects/vfx_tracers` (re-export prelude mais jamais wiré)
- `ForgiaInventoryPlugin` / `ForgiaQuestsPlugin` / `ForgiaXpCurvesPlugin` — `forgia-rpg-data` (`ForgiaRpgPlugin` est wiré mais ne charge pas ces sub-plugins → features RPG silencieusement absentes)

### Meta-bundles morts (sub-plugins wirés directement à la place)
- `ForgiaJuiceLibPlugin` — `forgia-juice-lib` (game wire les 4 sub-plugins individuellement)
- `ForgiaUiLibPlugin` — `forgia-ui-lib` (game wire 5 sub-plugins individuellement)

### Plugins définis dans crates 100% orphelines (cf §1)
- `ForgiaWeaponHitscanPlugin`, `ForgiaPpToonPlugin`, `ForgiaPpOutlinePlugin`
- `ForgiaRigTopologyPlugin`, `ForgiaIkPlugin` (crates `forgia-rig-topology`, `forgia-ik` — non-orphelines Cargo mais Plugins jamais activés)

---

## 3. Double-add panic-risk (Bevy 0.18, sans `is_plugin_added::<>()` guard)

⚠️ **Reference** : [reference_plugin_idempotent_guard.md](../../../../d--Forgia/memory/feedback_plugin_idempotent_guard.md)

| Plugin | Site A | Site B | Risque |
|---|---|---|---|
| **ForgiaDamagePlugin** | `forgia-game/src/lib.rs:65` (unguarded) | `forgia-ai-arena-bot/src/lib.rs:167` (unguarded) | 🔴 panic si chaîne game → mode-fps-arena → ai-arena-bot |
| **ForgiaJuiceRecoilPlugin** | `forgia-combat/src/lib.rs:135` (unguarded) | `forgia-fps/src/lib.rs:301` (unguarded) | 🔴 panic — game wire combat ET fps |
| MeshFaderPlugin | `forgia-fps:287` (unguarded) | `forgia-viewmodel:75` (guarded) | 🟡 viewmodel guard sauve, mais asymétrique |

---

## 4. Mono-consumer + grosse surface API (candidats fusion vague suivante)

Pattern récurrent post-fusion (story-513) : crate exporte beaucoup de pub items mais 1 seul consumer.

| Crate | pub items | Consumer unique | Note |
|---|---|---|---|
| forgia-stage | 27 | forgia-mode-roguelite | candidat fusion |
| forgia-fps | 19 | forgia-game | candidat fusion |
| forgia-village-kit | 13 | forgia-village-loader | candidat fusion |
| forgia-auto-rig | 13 | forgia-rpg | candidat fusion |
| forgia-level-presets | 11 | forgia-stage | candidat fusion |

---

## 5. Mémoires périmées détectées

Citées dans MEMORY.md V1 mais inexistantes dans workspace V2 (déjà fusionnées) :
- `forgia-stage-arena` → fusionnée dans `forgia-stage`
- `forgia-stage-graph` → fusionnée dans `forgia-stage::graph`
- `forgia-loot-tables` → fusionnée dans `forgia-rpg-data::loot_tables`
- `forgia-anchor-kit` → fusionnée dans `forgia-anchor`

À nettoyer dans MEMORY.md V2.

---

## Stories de remédiation (DRAFT créées)

| ID | Scope | Priorité |
|---|---|---|
| story-542 | Fix 2 double-add panic-risk (ForgiaDamagePlugin + ForgiaJuiceRecoilPlugin) | 🔴 P0 — panic potentiel runtime |
| story-543 | Re-câbler 3 plugins rpg-data (Inventory + Quests + XpCurves) dans ForgiaRpgPlugin | 🟠 P1 — features silencieuses |
| story-544 | DELETE/re-wire 3 crates orphelines (weapon-hitscan + postprocess + spline) | 🟡 P2 — code mort |

---

## Sources

- Output Cargo deps : agent `general-purpose` 2026-05-27 (62 packages metadata)
- Output Plugin wiring : agent `general-purpose` 2026-05-27 (49 Plugins, scan `impl Plugin for *Plugin` vs `add_plugins(*)`)
- Output pub items : agent `general-purpose` 2026-05-27 (59 lib.rs scannés)
